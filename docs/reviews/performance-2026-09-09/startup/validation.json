{
  "date": "2026-09-09",
  "platform": "Windows",
  "cpu": "AMD Ryzen 7 3800X",
  "productChange": "Only unconstrained command-contract schemas bypass Ajv compilation. Constrained schemas retain isolated eager compilation; drafts, worker reads and recovery code are unchanged.",
  "fullSuite": {
    "command": "npm test",
    "passed": 2624,
    "skipped": 4,
    "failed": 0,
    "durationMs": 153760.4789,
    "scope": "Includes command/Build behavior, API validation, daily Work, recovery, ownership, worker cancellation/cache invalidation and all performance regressions."
  },
  "focused": {
    "command": "node --test tests/performanceMetrics.test.js tests/performanceContract.test.js",
    "passed": 8,
    "failed": 0,
    "newCoverage": [
      "Real Build registration makes zero Ajv compile calls for its unconstrained contracts; legacy command diagnostics and disposal remain intact",
      "All JSON values accepted by empty/true schemas; false and constrained schemas still reject; invalid schemas fail eagerly; identical schema IDs remain isolated and external references fail closed",
      "CPU phase clipping and recursive-inclusive de-duplication",
      "Event Timing groups the longest event per interaction and excludes unrelated phases"
    ]
  },
  "productionBuild": "Passed, including hidden source-map build; existing 523.18 kB DailyRoot warning remains",
  "lint": "npm run lint passed: six stacks",
  "dependencies": "npm install --package-lock-only --ignore-scripts --offline passed; trace-mapping is now an explicit development dependency",
  "syntax": "node --check passed for both profiling scripts, including the final one-scenario-per-window guard",
  "coreGate": {
    "command": "npm run perf:check",
    "counts": [10, 100, 500],
    "warmRepeatsPerEndpoint": 5,
    "failed": 0,
    "report": "core-check.json",
    "result": "Cold list reads performed zero main-process session reads and summary-body opens; unchanged warm reads performed zero body reads in either process.",
    "remainingLimit": "500-run warm history median was 152.79 ms, still above the previously proposed 100 ms latency target. This run gates rereads, not that optional latency target."
  },
  "electronComparisons": {
    "report": "comparison.json",
    "passed": 12,
    "repetitionsPerVariantPerFixture": 3,
    "fixtures": [
      { "runs": 100, "chunksPerRun": 200, "toolBytesPerRun": 0 },
      { "runs": 10, "chunksPerRun": 2000, "toolBytesPerRun": 2097152 }
    ],
    "assertions": "Exact typed drafts, input delivery during concurrent history and snapshot/log reads, actual activity rendering, detail expansion/collapse, draft retained after detail toggles, exactly two shared polls in eleven seconds",
    "invalidExcluded": { "report": "invalid-occluded.json", "reason": "document.hidden was true; zero polls was correct visibility behavior. Final comparisons kept the synthetic window always on top." }
  },
  "profiles": {
    "pairedRecordings": 4,
    "processes": ["main", "renderer"],
    "phases": ["startup", "run opening", "reply typing"],
    "archive": "startup-profiles.zip",
    "includes": "Raw cpuprofiles, Chromium timelines, unchanged production renderer JavaScript and matching hidden source maps",
    "commandSmoke": "npm run perf:profile passed with Event Timing, mapped CPU summaries and clock-aligned Chromium summary; additional local output .flyt/performance/profile-command-smoke.json"
  },
  "limitations": [
    "Reply typing target remains unmet; presentation and GPU raster/flush work require further attribution before another optimization",
    "Startup maximum gaps remain roughly 181–214 ms in valid after repetitions",
    "Event Timing is thresholded and quantized; these small-sample interaction percentiles are not INP",
    "No special React profiling-build commit timings",
    "No installed-release, slower-hardware, live-stream or soak measurements; remote CI was not run"
  ]
}
